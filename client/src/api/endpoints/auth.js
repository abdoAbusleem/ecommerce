const USERS = '/Users';
const REGISTER = '/add-User';
const LOGIN = '/login';
const LOGOUT = '/logout';

export default {
    REGISTER_ENDPOINT: USERS + REGISTER,
    LOGIN_ENDPOINT: USERS + LOGIN,
    LOGOUT_ENDPOINT: USERS + LOGOUT,
}